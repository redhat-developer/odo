environments:
  - name: development
    services:
      - name: service-1
        source_url: https://github.com/myproject/myservice.git
        webhook:
          secret:       # secret is missing 
        pipelines:
          integration:  # templates and bindings are missing
    apps:
      - name: app-1
        services:
          - service-1