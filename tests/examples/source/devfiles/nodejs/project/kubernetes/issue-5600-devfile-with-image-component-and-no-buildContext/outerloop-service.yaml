apiVersion: v1
kind: Service
metadata:
  labels:
    app: devfile-nodejs-deploy
  name: devfile-nodejs-deploy
spec:
  ports:
    - name: http-3000
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: devfile-nodejs-deploy
  type: LoadBalancer