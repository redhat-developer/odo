apiVersion: registry.devfile.io/v1alpha1
kind: DevfileRegistriesList
metadata:
  name: namespace-list
spec:
  devfileRegistries:
    - name: devfile-staging
      url: 'https://registry.stage.devfile.io'
    - name: second-devfile-reg
      url: 'https://registry.devfile.io'
      skipTLSVerify: true
