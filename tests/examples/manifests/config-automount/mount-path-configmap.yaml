apiVersion: v1
kind: ConfigMap
metadata:
  name: automount-mount-path-configmap
  labels:
    devfile.io/auto-mount: "true"
  annotations:
    devfile.io/mount-path: "/mnt/mount-path/configmap"
data:
  foo2: bar2
  ping2: pong2
