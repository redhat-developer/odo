apiVersion: v1
kind: Secret
metadata:
  name: automount-access-mode-subpath-secret
  labels:
    devfile.io/auto-mount: "true"
  annotations:
    devfile.io/mount-path: "/etc"
    devfile.io/mount-as: subpath
    devfile.io/mount-access-mode: "0444"
stringData:
  secret0444: "5ecr3t"
