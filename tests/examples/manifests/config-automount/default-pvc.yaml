apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    devfile.io/auto-mount: "true"
  name: automount-default-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
