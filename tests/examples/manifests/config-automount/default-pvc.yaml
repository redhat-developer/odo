apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    controller.devfile.io/mount-to-containers: "true"
  name: automount-default-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
