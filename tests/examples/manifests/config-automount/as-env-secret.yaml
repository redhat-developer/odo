apiVersion: v1
kind: Secret
metadata:
  name: automount-env-secret
  labels:
    devfile.io/auto-mount: "true"
  annotations:
    devfile.io/mount-as: env
stringData:
  code4: "4567"
  secret4: "PassWd4"
