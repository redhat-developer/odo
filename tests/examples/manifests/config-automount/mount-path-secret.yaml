apiVersion: v1
kind: Secret
metadata:
  name: automount-mount-path-secret
  labels:
    controller.devfile.io/mount-to-containers: "true"
  annotations:
    controller.devfile.io/mount-path: "/mnt/mount-path/secret"
stringData:
  code2: "2345"
  secret2: "PassWd2"
