apiVersion: v1
kind: Secret
metadata:
  name: automount-mount-path-secret
  labels:
    devfile.io/auto-mount: "true"
  annotations:
    devfile.io/mount-path: "/mnt/mount-path/secret"
stringData:
  code2: "2345"
  secret2: "PassWd2"
