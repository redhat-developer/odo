apiVersion: v1
kind: Secret
metadata:
  name: automount-subpath-secret
  labels:
    controller.devfile.io/mount-to-devworkspace: "true"
  annotations:
    controller.devfile.io/mount-path: "/mnt/subpaths"
    controller.devfile.io/mount-as: subpath
stringData:
  code5: "5678"
  secret5: "PassWd5"
